name: web-native-fetch
version: 1.0.0
description: "Local HTTP fetch + readability extract — no API key required. Uses httpx with readability-lxml for main-content extraction and html2text for markdown conversion. Extract-only (pair with any search provider)."
author: VHSgunzo
kind: backend
provides_web_providers:
  - native