[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.agent-control-worker"

[deploy]
startCommand = "node scripts/agent-control-worker.mjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

