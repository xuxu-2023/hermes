"""Integration exemplars.

These files demonstrate **the exact shape** a hermes-agent maintainer needs
to add to wire the runtime kernel into the existing codebase. They are
**not imported by the kernel itself** — they are templates to copy into a
hermes-agent fork and adapt.

Each file has TODO markers where hermes-internal types/calls plug in.
"""
