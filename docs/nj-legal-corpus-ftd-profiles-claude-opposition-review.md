# Claude Opposition Review — NJ Legal Corpus FTD Profiles Spec

Source review file: `/tmp/claude_ftd_profiles_review.md`

The review below was generated by Claude Code CLI in print mode after the first attempt timed out at 300s and the second bounded attempt completed.

---

# Adversarial Review: NJ Legal Corpus FTD Profiles Spec
Reviewer: Claude (claude-sonnet-4-6)
Date: 2026-05-15
Spec: `/Users/johngalt/.hermes/hermes-agent/docs/nj-legal-corpus-ftd-profiles-spec.md`

## 1. Verdict

**Needs revision.**

The spec accurately describes Hermes mechanics and verified real code. However it contains one silent data-loss risk (`git worktree add -B`), two contradictory recommendations (Hindsight bank_id vs template), a complete operational gap in cross-profile Kanban board ownership, vague FTD loop start instructions, and missing behavioral specs for the component FTD SOUL.md. Nothing here is a hard blocker if fixed before execution, but several items will cause silent failures or confusing behavior if encountered cold by an autonomous agent.

## 2. Critical Findings

- `git worktree add -B` silently resets an existing branch; use lowercase `-b` or document the risk.
- `--clone` copies the default profile `.env`, including active Discord bot token; clear inherited token immediately.
- `--clone` source profile was not specified; verify default/Galt profile before clone.
- Kanban board ownership/task creation workflow was not specified.
- Hindsight bank template/static recommendations conflicted.

## 3. Important Findings

- FTD loop start command hedged instead of naming a canonical command.
- `max_sprints: 1` needed explanation as first-launch safety, not steady-state autonomy.
- Profile wrapper alias use should be avoided or verified.
- Hindsight API pre-flight was missing.
- Checkpoint notification mechanism was undefined.
- Component FTD `SOUL.md` template was missing.
- Watchdog/liveness policy for multiple FTDs needs attention later.
- No-push-to-main needs branch protection for technical enforcement.

## 4. Minor Findings

- Token presence check had false-positive empty-token case.
- `--clone` copies `memories/USER.md`; identity contamination risk should be acknowledged.
- `hermes gateway list` scope should be clarified by additional per-profile status checks.
- Discord intents should be explicit.
- Worktree post-merge lifecycle still needs future policy.
- Hindsight timeout/concurrency should remain bounded.

## 5. Resulting changes applied to final spec

The final spec was revised to address the actionable findings:

- changed worktree creation from `-B` to `-b`
- added immediate post-clone clearing of inherited Discord tokens
- required verifying default/Galt profile before cloning
- chose static Hindsight bank IDs instead of templates
- added Hindsight `/version` and localhost bind pre-flight
- replaced profile aliases with explicit `hermes -p <profile>` commands
- corrected `ftd_start.py` command to use positional repo path
- added Kanban ownership and checkpoint notification protocol
- added component `SOUL.md` minimum template
- fixed token presence verification script
- added branch-protection risk note
- made Discord intents explicit from Hermes Discord adapter code
